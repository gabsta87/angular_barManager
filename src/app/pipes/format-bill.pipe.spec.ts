import { FormatBillPipe } from './format-bill.pipe';

describe('FormatBillPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatBillPipe();
    expect(pipe).toBeTruthy();
  });
});
