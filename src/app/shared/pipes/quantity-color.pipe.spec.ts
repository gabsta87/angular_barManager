import { QuantityColorPipe } from './quantity-color.pipe';

describe('QuantityColorPipe', () => {
  it('create an instance', () => {
    const pipe = new QuantityColorPipe();
    expect(pipe).toBeTruthy();
  });
});
