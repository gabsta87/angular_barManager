import { IonicButtonColorPipe } from './ionic-button-color.pipe';

describe('IonicButtonColorPipe', () => {
  it('create an instance', () => {
    const pipe = new IonicButtonColorPipe();
    expect(pipe).toBeTruthy();
  });
});
