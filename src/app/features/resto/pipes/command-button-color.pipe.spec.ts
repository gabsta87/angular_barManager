import { CommandButtonColorPipe } from './command-button-color.pipe';

describe('CommandButtonColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CommandButtonColorPipe();
    expect(pipe).toBeTruthy();
  });
});
